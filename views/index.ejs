<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
    <title>Socket Server</title>
    <script src="http://gambit.local:3000/socket.io/socket.io.js"></script> 
    <script> 
      var socket = new io.Socket(); 
      var world;
      socket.on('connect', function() { console.log("Connection to server has been established.") }) 
      socket.on('message', function(message) { world = message }) 
      socket.on('disconnect', function() { console.log("The connection has been closed!") }) 
    </script> 
  </head>
  <body>
    :-)
  </body>
</html>
